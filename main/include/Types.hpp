#pragma once

#include <stdint.h>

/**
 * @file Types.hpp
 * @brief General type definitions and constants.
 * @details This file defines common types and constants used throughout the project.
 */
const size_t GEN_BUFFER_SIZE = 1024;
const size_t GEN_STRING_SIZE = 32;
const size_t GEN_LINE_SIZE = 256;